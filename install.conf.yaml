- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: 
        path: nvim
    ~/.gitconfig:
        path: gitconfig/.gitconfig

- shell:
    - [git submodule update --init --recursive, Installing submodules]

