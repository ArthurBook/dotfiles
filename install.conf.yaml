- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: 
        path: nvim
        exclude: [ nvim/test ]
    ~/.gitconfig:
        path: gitconfig/.gitconfig

- shell:
    - [git submodule update --init --recursive, Installing submodules]

